#pragma OPENCL EXTENSION cl_khr_fp64: enable

// Other Sources Required
{{other_sources}}

// Specific Map Lambda Function
{{filter_lambda_def}}

__kernel void {{filter_kernel_name}}(__global const {{type}}* filter_input, __global {{type}}* filter_output {{extra_args}}) {
	int filter_global_id = get_global_id(0);
	filter_output[filter_global_id] = {{filter_lambda_name}}({{get_input}} {{extra_args_call}});
}
